export const BaseNavigationLink = (icon, text, href) => {
    return (
        <span>
            <Icon icon={icon} /> &nbsp;
            <a href={href} target="_blank" rel="noreferrer">
                {text}
            </a>
        </span>
    );
}